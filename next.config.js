/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Experimental options removed; Next.js 15 enables the app directory by default.
};

module.exports = nextConfig;